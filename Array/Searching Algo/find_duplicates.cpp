/* You are given array 'ARR' of size 'n' containing each number between 1 and 'N' 1 at least once
There is a single integer value that is present in the array twice. You task is to find duplicate
intger value present in the array
*/
#include<iostream>
#include<vector>
using namespace std;
int main(){

    vector<int> arr;
    for(int i=0; i<arr.size(); i++){
        cin>>arr[i];
    }
    //}
    //int ans = 0;
    //for(int i)
//
    return 0;
}
